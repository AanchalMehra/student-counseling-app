services:
  - type: web
    name: counseling-app
    env: node
    plan: free
    # This tells Render that your backend code is in the 'server' folder
    rootDir: server
    buildCommand: "npm install && npm install --prefix ../client && npm run build --prefix ../client"
    startCommand: "npm start"